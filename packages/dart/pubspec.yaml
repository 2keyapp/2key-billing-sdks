name: two_key_dart_sdk
description: 2key Billing Dart SDK (interim pure-Dart; FRB → two-key-core planned)
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  dart_jsonwebtoken: ^3.2.0
  http: ^1.2.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
