{
  "name": "@2key/javascript",
  "private": true,
  "version": "0.1.0",
  "description": "2key JavaScript / TypeScript SDK workspace — AuthN + AuthZ + Billing",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck",
    "ci": "pnpm build && pnpm typecheck && pnpm test"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.15.0",
  "license": "Apache-2.0"
}
