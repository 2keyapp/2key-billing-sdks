# @2key/ts-sdk

Browser **behavioral reference** for 2key Billing (cookies / redirect auth).

- Same OpenAPI + conformance fixtures as `two-key-core`
- **Not** a WASM wrap of Rust
- Host apps import `@2key/ts-sdk` only

See `docs/auth-protocol.md` and `docs/sdk-conformance.md`.
