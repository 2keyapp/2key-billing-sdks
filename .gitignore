# Fetched core / CLI binaries (Binary Private Core)
/bin/
!bin/.gitkeep

# Rust (local only — source lives in private 2key-core-sdk)
/target/
**/*.rs.bk

# Node / TS
node_modules/
packages/ts/dist/
packages/ts/*.tsbuildinfo
.npm/

# Dart / Flutter
packages/dart/.dart_tool/
packages/dart/build/
packages/dart/.packages
packages/dart/pubspec.lock

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Secrets
*.pem
!conformance/fixtures/**/*.pem
!packages/dart/keys/**/*.pem
.env
.env.*
